[project]
name = "llm-compression-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lukas Bannenberg", email = "lukasbannenberg@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.1",
    "physics-tenpy>=1.1.0",
    "torch>=2.13.0",
    "transformers>=5.14.1",
]

[project.scripts]
llm-compression-demo = "llm_compression_demo:main"

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
